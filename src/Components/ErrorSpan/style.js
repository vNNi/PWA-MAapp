import Colors from "../../Colors";
export default {
    container: { color: 'white', padding: '', fontFamily: 'Roboto', fontSize: '16px', fontWeight: 'bold' },
    errorWraper: {
        marginBottom: '1em',
        backgroundColor: Colors.reject,
        borderRadius: '4px',
        textAlign: 'center',
        padding: '8px',
    }
}